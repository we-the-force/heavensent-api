{
    "routes": [
        {
            "method": "POST",
            "path": "/stripeNotifications",
            "handler": "stripeNotification.receiveEvents",
            "config": {
                "policies": []
            }
        }
    ]
}